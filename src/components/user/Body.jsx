import { SlideContainer } from "../../components"

const Body = ({ children }) => {

    return (
        <main className='w-full mt-20'>
            {/* <SlideContainer /> */}
            {children}
        </main>
    )
}

export default Body
